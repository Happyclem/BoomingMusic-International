# International-edition screenshots

Drop the multilingual-lyrics screenshots referenced by the main README here:

- `stacked_translations.png` — lyrics with original + multiple translations stacked
- `language_picker.png` — the globe button menu listing the file's languages
- `per_language_colors.png` — Settings → Lyrics → Translation colors (and/or colored lyrics)
- `per_language_color_picker.png` — the per-language HSV color wheel picker with hex input
- `save_local_file.png` — the lyrics editor "Export to file" menu (save lyrics outside the app)
- `settings_search.png` — the settings search screen

Capture them from a device/emulator (e.g. `adb exec-out screencap -p > stacked_translations.png`)
and commit the PNGs next to this file.
